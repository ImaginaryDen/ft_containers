#ifdef VECTOR_H
#define VECTOR_H

#include <memory>

namespace ft{

template< class T, class Allocator = std::allocator<T>>class vector{

	/* TODO:
	 * (constructor)
	 * (destructor)
	 * operator=
	 * assign
	 * get_allocator
	 * at
	 * operator[]
	 * front
	 * back
	 * data
	 * Iterator
	 * Iterator access
	 * empty
	 * size
	 * max_size
	 * reserve
	 * capacity
	 * clear
	 * insert
	 * emplace
	 * erase
	 * push_back
	 * pop_back
	 * resize
	 * swap
	 * Non-member functions
	 * erase*/
};
}

#ifndef //VECTOR_H